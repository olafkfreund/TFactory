"""Java language primitives for the TFactory Evaluator (#237, epic #232).

Mirrors the Python (``agents/``) and TypeScript (``agents/lang_typescript/``)
primitives for the first compiled-language wedge:
  - ``mutate_probe`` — JUnit-assertion mutation (PIT-backed at runtime).
  - ``jacoco_coverage`` — parse a JaCoCo XML report into the coverage contract.
"""
