# Scenario metadata for /demo and scripts/demo/seed-scenario.sh.
# Sourced as shell; keep it `KEY=value` with no spaces around `=`.
SCENARIO_ID=python-unit
PROJECT_ID=tfactory-demo-python
PROJECT_NAME="TFactory Demo — Pricing Helper"
SPEC_ID=001-pricing-helper
SPEC_TITLE_PREFIX="# Pricing Helper"
LANES="unit,mutation"
EXPECT_FAILURE=1
BASE_REF=main
BRANCH=tfactory/demo-python-unit
# pyproject is written by the seed so the SUT is a real Python project root.
PROJECT_KIND=python
